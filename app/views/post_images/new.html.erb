<!--投稿フォーム-->
<h1>画像投稿フォーム</h1>

<% if @post_image.errors.any? %><!--createアクションでエラー起きた場合は-->
  <%= @post_image.errors.count %>件のエラーが発生しました<!--エラー数を数えて表示-->
  <ul>
    <% @post_image.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>



  <%= form_with model: @post_image do |f| %>

  <h4>画像</h4>
    <%= f.file_field :image, accept: "image/*" %>

  <h4>ショップ名</h4>
    <%= f.text_field :shop_name %>

  <h4>説明</h4>
    <%= f.text_area :caption %>

  <%= f.submit '投稿' %>

  <% end %>